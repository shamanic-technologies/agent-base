{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": [
      "apps/key-service/**",
      "packages/**",
      "package.json",
      "turbo.json"
    ],
    "nixpacks": {
      "buildCmd": "cd ../../ && npm i -g pnpm@8.15.8 && pnpm install && pnpm run build"
    }
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "node dist/index.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 10,
    "restartPolicyType": "ON_FAILURE"
  }
} 