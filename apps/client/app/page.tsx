'use client';

import { ChatUI } from '../components/chat-ui';

/**
 * Main Chat Application Page
 * Using our modern ChatUI component with shadcn styling
 * The ChatUI component uses fixed positioning to take over the entire viewport
 */
export default function Home() {
  return <ChatUI />;
} 