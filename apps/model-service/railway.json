{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": [
      "**/*.ts",
      "package.json",
      "tsconfig.json"
    ],
    "nixpacks": {
      "buildCmd": "npm i -g pnpm && pnpm install --no-frozen-lockfile && pnpm build"
    }
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "pnpm start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 10,
    "restartPolicyType": "ON_FAILURE"
  }
} 