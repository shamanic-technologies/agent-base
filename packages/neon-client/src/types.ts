/**
 * Types for the Neon client package
 */
export interface NeonProject {
    id: string;
    name: string;
    created_at: string;
}

export interface ConnectionURI {
    connection_uri: string;
} 