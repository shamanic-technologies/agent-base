{
  "name": "@agent-base/credentials",
  "version": "0.1.0",
  "description": "Shared credentials types and utilities",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "lint": "eslint --ext .ts src",
    "test": "jest"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.4",
    "rimraf": "^5.0.0"
  }
} 